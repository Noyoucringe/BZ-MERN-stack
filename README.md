# BZ-MERN-stack

Repo created as part of a fullstack training bootcamp.

## Local development

### Backend

```bash
cd backend
npm install
npm run start
```

### Frontend

```bash
cd frontend
npm install
npm run dev
```

## Notes

- The backend serves APIs under `/api/*`.
- Auth uses an HTTP-only cookie named `jwt`.
